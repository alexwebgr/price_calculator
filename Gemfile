source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '3.0.0'

gem 'dotenv'
gem 'sinatra'
gem 'sinatra-contrib'

group :development, :test do
  gem 'rspec'
  gem 'simplecov'
  gem "webmock"
end
